---
type: index
updated: 2026-08-18
---

# 2. 논문·프로젝트

`2. 논문·프로젝트` 폴더의 프로젝트 목록. 각 항목은 해당 프로젝트의 담당 노트(폴더와 동일한 이름)로 링크된다. 새 프로젝트 폴더가 생기면 다음에 열 때 자동으로 반영된다 — 직접 수정할 필요 없음.

```dataviewjs
const overviews = dv.pages('"2. 논문·프로젝트"')
  .where(p => p.file.name === p.file.folder.split("/").pop())
  .sort(p => p.file.folder)

dv.list(overviews.map(p => p.file.link))
```
