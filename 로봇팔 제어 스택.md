

## 1. 로봇팔 제어의 전체 계층 구조

```
[7] 애플리케이션 로직        무슨 작업을 할지 (픽앤플레이스 시퀀스, 비주얼 서보잉 루프)
[6] 모션 플래닝              충돌 없이 어디로 갈지 (경로 탐색)
[5] 궤적 생성                그 경로를 시간에 따라 어떻게 지나갈지 (속도/가속도 프로파일)
[4] 태스크/모션 제어         그 궤적을 추종하기 위한 관절 명령 계산 (IK, 임피던스, QP 등)
[3] 동역학 모델               명령을 실제 토크로 바꾸기 위한 물리 모델 (M, C, g)
[2] 기구학 모델               관절각 ↔ TCP 위치 변환 (FK, Jacobian)
[1] 관절 서보 / 전류 루프     모터가 실제로 그 토크/속도를 내게 함
[0] 필드버스 / HW 통신        제어 PC ↔ 모터 드라이버 간 실시간 데이터 교환
```

- **[0] 필드버스**: EtherCAT, CANopen, PROFINET 등 산업용 실시간 통신 규격. 마이크로초 단위 지터로 다축 동기 제어를 가능하게 함.
- **[1] 서보 루프**: 전류 → 속도 → 위치 순 캐스케이드 구조, 보통 모터 드라이버 자체가 담당.
- **[2] 기구학**: FK(관절각→TCP), IK(TCP→관절각), Jacobian(순간 속도 관계).
- **[3] 동역학**: `M(q)q̈ + n(q,q̇) = τ` — 토크 제어, 중력 보상, 임피던스 제어의 기반.
- **[4] 태스크 제어**: 단순 Jacobian 역행렬 기반 resolved-rate control부터, 임피던스 제어, QP 기반 다중 태스크 조율까지 다양한 방식 존재.
- **[5] 궤적 생성**: 사다리꼴/S-curve/minimum-jerk 프로파일 등으로 시간에 따른 목표점 시퀀스 생성.
- **[6] 모션 플래닝**: RRT, RRT*, PRM 등으로 충돌 없는 경로 탐색.
- **[7] 애플리케이션**: 전체 작업 시나리오(상태 머신).

---

## 2. QP 기반 다중 태스크 조율 (OpenSoT류)이란

### 2.1 문제의식 — 단일 태스크 제어의 한계

전통적인 Jacobian 기반 제어(`q̇ = J⁺ẋ`)는 **하나의 목표**(예: TCP 위치 추종)에는 적합하지만, 다음과 같은 상황에서 한계가 생긴다:

- 목표가 여러 개 동시에 존재 (엔드이펙터 위치 + 무게중심 유지 + 관절 중립 자세 등)
- 목표들 사이에 **우선순위**가 필요 (엔드이펙터 위치는 반드시 지키고, 나머지는 여유 안에서만)
- **물리적 한계**(관절/토크/속도 리밋)를 동시에 만족시켜야 함
- 로봇 구조(자유도)가 바뀌어도 제어기 구조를 재사용하고 싶음

이런 요구를 만족시키기 위해 등장한 것이 **QP(Quadratic Programming) 기반 다중 태스크 조율 프레임워크**이며, OpenSoT가 대표적인 구현체다.

### 2.2 기본 구조

**태스크(Task)** = 가중 최소자승 비용함수로 표현

$$\Gamma(x) = |Ax - b|_W^2$$

- `x`: 최적화 변수 (관절 속도, 가속도, 토크 등)
- `A, b`: 목표를 나타내는 행렬/벡터 (예: Jacobian과 목표 속도)
- `W`: 가중치 행렬 — 어떤 축/방향을 더 중요하게 볼지 조절

**제약(Constraint)** = 선형 부등식

$$l \le Cx \le u$$

- 관절 한계, 토크 한계, 속도 한계 등 "절대 어기면 안 되는 규칙"을 표현

### 2.3 다중 태스크 조율 방식

**(1) Soft Priority — 비용함수 가중합**

- 여러 태스크를 "동등하게 중요"하다고 보고, 각 비용함수에 가중치를 곱해 하나의 큰 비용함수로 합침.
- 장점: 구현이 간단, 여러 목표를 부드럽게 절충.
- 단점: 목표들이 충돌하면 어느 것도 완벽히 만족되지 않고 타협된 값이 나옴.

**(2) Hard Priority — Null-space 투영**

- 우선순위를 명확히 나눔: 1순위 태스크를 먼저 최적으로 풀고, **그 해를 건드리지 않는 여유 공간(null-space)** 안에서만 2순위 태스크를 추가로 최적화.
- 예: "엔드이펙터 위치는 반드시 정확히"(1순위, hard) + "여유 자유도로 관절을 편한 자세로"(2순위, postural task).
- 장점: 중요한 목표가 절대 희생되지 않음.
- 단점: 구현이 soft priority보다 복잡 (null-space 투영 수식 필요).

두 방식 모두 OpenSoT의 **Stack of Tasks** 구조 안에서 조합 가능 — 태스크들을 "쌓아 올려서(stack)" 우선순위 순으로 처리.

### 2.4 왜 특이점(singularity) 문제에도 더 안정적인가

- 순수 Jacobian 역행렬(`J⁻¹`, `J⁺`) 방식은 특이점 근처에서 값이 발산 → 관절 명령 폭주 → 로봇이 "튐".
- QP 기반 방식은:
    1. 관절/속도/토크 한계를 **부등식 제약으로 원천 차단** — 발산하는 해가 나와도 그대로 출력되지 않음.
    2. 최소자승 형태 자체가 감쇠 최소자승(damped least squares)과 유사한 완충 효과.
    3. Hard priority 구조에서, 특이점 때문에 특정 방향이 불가능해지면 그 방향은 "포기"하고 null-space 안에서 타협된 해를 찾음.
- 다만 특이점 자체가 사라지는 것은 아니며, 여전히 Jacobian 기반이라 특이점 근처 정밀 제어력은 떨어질 수 있음. QP 구조의 이점은 "안전하게 타협"이지 "특이점 회피"가 아님.

### 2.5 계층에서의 위치 정리

```
CartesI/O 류 (궤적 생성)   : "지금부터 이 경로를 따라가라" → WHAT to track
        ↓
OpenSoT (QP 기반 태스크 조율): "그 경로를 따라가면서, 제약도 지키고 우선순위도 지켜라" → HOW to satisfy multiple goals
        ↓
XBot / 미들웨어 (HW 통신)   : 계산된 명령을 실제 로봇에 실시간 전달
```

- **궤적 생성**과 **다중 태스크 QP 조율**은 서로 다른 계층: 전자는 "목표 시퀀스"를 만들고, 후자는 "매 순간 그 목표(들)를 어떻게 동시에 만족시킬지"를 계산.
- 로봇 자유도가 바뀌어도(모듈 추가/제거) QP 행렬 `A, C`의 **크기만 자동으로 바뀔 뿐**, 태스크 정의·우선순위 구조·파라미터는 그대로 재사용 가능 — 재구성 가능 로봇에 특히 유리한 이유.

### 2.6 단순 Jacobian 기반 제어와의 비교

|항목|단순 Jacobian 역행렬 (예: RBDL 직접 구현)|QP 기반 다중 태스크 (OpenSoT류)|
|---|---|---|
|목표 개수|보통 단일 목표|여러 목표 동시 처리|
|우선순위|직접 구현 필요|Soft/Hard priority 내장|
|제약 처리|별도 클리핑/로직 필요|QP 부등식 제약으로 통합|
|특이점 대응|발산 위험 (직접 댐핑 등 구현 필요)|제약으로 발산 원천 차단 (완전 해결은 아님)|
|재구성 대응|자유도 변경 시 수동 수정 필요|행렬 크기만 자동 변경, 구조 재사용|
|궤적 생성|별도 구현 필요|별도 구현 필요 (OpenSoT 자체 기능 아님)|

---

## 3. 참고 — 예시로 본 순차적 구현 흐름

**① 물건 옮기기 (고정 시퀀스 작업)**

1. 애플리케이션: 상태 머신 정의 (접근→그립→이동→릴리즈)
2. 모션 플래닝: 장애물 없는 경로 탐색
3. 궤적 생성: 시간에 따른 속도/가속도 프로파일
4. 태스크 제어: 매 주기 IK/Jacobian(or QP)으로 관절 명령 변환
5. 동역학: 토크 계산 (중력 보상 포함)
6. HW: 필드버스로 실시간 전송

**② 비주얼 서보잉 (동적 반응 작업)**

1. 비전 처리: 카메라에서 목표물 위치/오차 추출
2. 사전 궤적 생성 없이, 매 프레임마다 목표 `x_d`를 실시간 갱신
3. 태스크 제어: 갱신된 목표에 대해 (image) Jacobian 기반 제어 반복 계산
4. 동역학/HW: 동일하게 실제 모터 구동

두 경우 모두 [4] 태스크 제어 계층에서 단일 Jacobian 방식을 쓸 수도, QP 기반 다중 태스크 조율을 쓸 수도 있음 — 목표가 하나이고 단순하면 전자로 충분하고, 여러 목표·제약·재구성 대응이 필요하면 후자가 유리.